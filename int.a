;--- Example code fragment, start ---

		!to "int.o", cbm	; set output file and format
		*= $55AA		; set program counter

		basout = $1234		; explicit global label def.
		pha
		lda	#$66
		sta basout
		pla
		rti
		nop
		nop
		nop
		nop
		nop
		!byte $FF	; End of emulation 



;string:		
;		!text "Dumb example", 13, 0

;--- Example code fragment, end ---
